es una fucion que nos sirve para dar una alerta con el parametro que le ingresemos
alert(parametro);
js lenguaje interpretado
DOM(document Object Model) lo representa para js

una interaccion con una pagina que estamos desarrollando podriamos llamarla un evento

dato
on---....
podemos usar diferentes funciones que sirve para que usuario interactue con la pagina


hosting (buscar que es en js)

fijarse en la url si se puede cambiar el idioma /US/ -> /es/

Template strings, o plantillas de cadenas
const mensaje = `Hola, soy ${nombre} y tengo ${edad} años.`;
